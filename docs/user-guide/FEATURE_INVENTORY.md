# Feature inventory

Generated by `python scripts/feature_inventory.py --markdown`.
Every row is checked against the source tree, so a feature that quietly
disappears during a refactor turns this file's generator red.

**91 features proven.**

## Accounts

| Feature | Proven by |
| --- | --- |
| Multi-account dashboard in one joined snapshot | `app/backend/services/application_service.py` → `def get_dashboard`, `app/frontend/src/app.js` → `renderDashboard()` |
| Live state: offline / launching / in game / unattended / error | `app/backend/services/application_service.py` → `return "afk"`, `app/backend/services/application_service.py` → `return "error"`, `app/frontend/src/app.js` → `afk: 'In game, unattended'` |
| Session runtime per instance | `app/backend/services/application_service.py` → `_instance_runtime_seconds` |
| PID and memory per instance | `app/backend/services/application_service.py` → `"memory_mb"`, `app/backend/api/bridge.py` → `def get_instance_monitor` |
| Avatar, username and display name from the public profile | `app/backend/api/bridge.py` → `def get_public_profile`, `app/backend/api/bridge.py` → `def refresh_account_public_profile` |
| Roblox presence per account | `app/backend/api/bridge.py` → `def get_public_presence` |
| Account groups | `app/backend/api/bridge.py` → `def list_groups`, `app/backend/api/bridge.py` → `def move_accounts` |
| Private note per account | `app/frontend/src/app.js` → `id="account-notes"` |
| Filterable tags | `app/backend/core/account_health.py` → `def normalize_tags`, `app/backend/api/bridge.py` → `def update_account_tags` |
| Custom fields (Level, Gems, Trait, ...) | `app/backend/core/account_health.py` → `def validated_custom_fields`, `app/backend/api/bridge.py` → `def update_account_fields` |
| Priority 0-10 used by the rules engine | `app/backend/api/bridge.py` → `def set_account_priority`, `app/backend/services/fleet_features.py` → `def set_account_priority` |
| Account health: session expired, auth needed, cannot launch | `app/backend/core/account_health.py` → `def evaluate_account_health`, `app/backend/api/bridge.py` → `def get_account_health` |
| Health filters by tag, status and text | `app/backend/core/account_health.py` → `def matches_filters` |
| Bulk import of accounts | `app/backend/api/bridge.py` → `def import_bulk_accounts` |
| Bulk delete, bulk group move, bulk edit | `app/backend/api/bridge.py` → `def delete_accounts`, `app/backend/api/bridge.py` → `def reorder_accounts` |
| Session recovery and refresh | `app/backend/api/bridge.py` → `def refresh_account_session`, `app/backend/api/bridge.py` → `def export_account_sessions` |
| OAuth sign-in and browser sign-in | `app/backend/api/bridge.py` → `def start_oauth_login`, `app/backend/api/bridge.py` → `def start_manual_browser_login` |
| Add an account from a cookie | `app/backend/api/bridge.py` → `def add_account_from_cookie` |
| Account tools: password, email, display name, friends, blocks | `app/backend/api/bridge.py` → `def change_account_password`, `app/backend/api/bridge.py` → `def set_account_display_name`, `app/backend/api/bridge.py` → `def block_account_user` |
## Launching

| Feature | Proven by |
| --- | --- |
| Launch one account through the Windows protocol | `app/backend/api/bridge.py` → `def launch_account`, `app/backend/services/application_service.py` → `def launch_account` |
| Smart launcher plan (concurrency cap, delay between launches) | `app/backend/api/bridge.py` → `def plan_smart_launch`, `app/backend/api/bridge.py` → `def start_smart_launch` |
| Wave launch with a pause between waves | `app/backend/services/fleet_features.py` → `def start_wave_launch`, `app/backend/core/config.py` → `wave_pause_seconds` |
| Dynamic launch queue that waits for the previous wave | `app/backend/services/fleet_features.py` → `def _wave_ready_check`, `app/backend/watchers/comfort.py` → `def queue_gate` |
| Batch launch with cancel and live status | `app/backend/api/bridge.py` → `def start_batch_launch`, `app/backend/api/bridge.py` → `def cancel_batch_launch` |
| Named launch profiles (place, server, FPS, group) | `app/backend/automations/launch_profiles.py` → `def validated_profile`, `app/backend/api/bridge.py` → `def launch_with_profile`, `app/frontend/src/app.js` → `renderFleetProfiles()` |
| UWP packages and per-account clones | `app/backend/api/bridge.py` → `def launch_uwp_package`, `app/backend/api/bridge.py` → `def create_uwp_account_clone` |
| Multi-instance switch | `app/backend/api/bridge.py` → `def set_multi_instance`, `app/backend/api/bridge.py` → `def get_multi_instance_status` |
| Private server / VIP link launch | `app/backend/api/bridge.py` → `def launch_account_from_private_link`, `app/backend/api/bridge.py` → `def parse_vip_link` |
| Join a specific player's server | `app/backend/api/bridge.py` → `def find_player_server`, `app/backend/api/bridge.py` → `def get_player_presence` |
| Random server pick | `app/backend/api/bridge.py` → `def get_random_server` |
| Region resolution and region probing | `app/backend/api/bridge.py` → `def resolve_server_region`, `app/backend/api/bridge.py` → `def probe_server_regions` |
| Window positioning and capture per instance | `app/backend/api/bridge.py` → `def position_instance_window`, `app/backend/api/bridge.py` → `def capture_instance_window` |
| Close the Roblox beta home windows that steal focus | `app/backend/api/bridge.py` → `def close_beta_home_windows` |
## Servers

| Feature | Proven by |
| --- | --- |
| JobId inspector and visit history | `app/backend/roblox/server_registry.py` → `def inspect_history`, `app/backend/services/fleet_features.py` → `def record_server_visit` |
| Server blacklist | `app/backend/roblox/server_registry.py` → `def blacklist_add`, `app/backend/api/bridge.py` → `def update_server_blacklist` |
| Smart hopping that never picks a blacklisted server | `app/backend/roblox/server_registry.py` → `def pick_server`, `app/backend/api/bridge.py` → `def pick_best_server` |
| Region affinity when picking a server | `app/backend/services/fleet_features.py` → `def pick_best_server`, `app/frontend/src/app.js` → `id="fleet-server-region"` |
## Macros

| Feature | Proven by |
| --- | --- |
| Visual block editor (drag list, add and remove blocks) | `app/frontend/src/app.js` → `add-macro-block`, `app/frontend/src/app.js` → `renderMacroBlock` |
| Text DSL editor compiling to the same action tree | `app/frontend/src/app.js` → `Direct Astro DSL`, `app/backend/automations/macros.py` → `REPEAT` |
| Keyboard, mouse and text actions | `app/backend/automations/macros.py` → `"key_press"`, `app/backend/automations/macros.py` → `"mouse_click"` |
| Condition / Launch / Teleport / Restart blocks | `app/backend/automations/macros.py` → `CONTROL_ACTIONS`, `app/backend/automations/macros.py` → `CONDITION_CHECKS` |
| Randomised waits (min-max) | `app/backend/automations/macros.py` → `"max_milliseconds"` |
| Bounded loops and bounded nesting | `app/backend/automations/macros.py` → `MAX_REPEAT`, `app/backend/automations/macros.py` → `MAX_DEPTH` |
| Named subroutines | `app/backend/automations/macros.py` → `MAX_SUBROUTINES` |
| Key profiles remapped per machine | `app/backend/automations/macro_studio.py` → `def validated_key_profile`, `app/backend/api/bridge.py` → `def save_key_profile` |
| Per-account macro variables | `app/backend/automations/macro_studio.py` → `def validated_variables`, `app/backend/api/bridge.py` → `def update_macro_variables` |
| Step-by-step debugger reporting missing variables | `app/backend/services/fleet_features.py` → `def debug_macro`, `app/backend/api/bridge.py` → `def debug_macro` |
| Dry run that never touches a window | `app/backend/services/application_service.py` → `dry_run`, `app/backend/automations/macros.py` → `dry_run` |
| Profiler estimating a run before it starts | `app/backend/automations/macro_studio.py` → `def profile_macro` |
| Versioning with rollback | `app/backend/automations/macro_studio.py` → `def push_version`, `app/backend/automations/macro_studio.py` → `def rollback_version`, `app/backend/api/bridge.py` → `def rollback_macro` |
| Group macro start | `app/backend/services/fleet_features.py` → `def start_group_macro` |
| Run log and run history | `app/backend/api/bridge.py` → `def get_macro_run_log`, `app/backend/api/bridge.py` → `def list_macro_runs` |
| Macro resume after a relaunch | `app/backend/services/fleet_features.py` → `def _queue_macro_resume`, `app/backend/core/config.py` → `resume_after_relaunch` |
| pydirectinput delivery for a single Roblox window | `app/backend/automations/direct_input.py` → `pydirectinput` |
| Multi-window macros kept behind an explicit flag | `app/backend/core/config.py` → `multi_window_macros` |
| Emergency stop: every macro and queued launch at once | `app/backend/services/fleet_features.py` → `def emergency_stop`, `app/frontend/src/app.js` → `fleet-emergency-stop` |
| Stop every macro without confirmation | `app/backend/services/application_service.py` → `def stop_all_macros` |
## Coordination

| Feature | Proven by |
| --- | --- |
| Spread mode across several servers | `app/backend/automations/coordination.py` → `def spread_plan` |
| Main + followers | `app/backend/automations/coordination.py` → `def follower_plan` |
| Synchronised launch and teleport | `app/backend/automations/coordination.py` → `def sync_plan` |
| Internal party (same JobId, bounded size) | `app/backend/automations/coordination.py` → `def party_plan` |
## Comfort

| Feature | Proven by |
| --- | --- |
| Resource plan with per-profile FPS targets | `app/backend/watchers/resource_plan.py` → `PROFILE_IDLE`, `app/backend/api/bridge.py` → `def get_resource_plan` |
| Adaptive FPS setting | `app/backend/core/config.py` → `adaptive_fps_enabled` |
| FPS cap read, set and removed | `app/backend/api/bridge.py` → `def set_fps_cap`, `app/backend/api/bridge.py` → `def remove_fps_cap` |
| Memory watchdog thresholds | `app/backend/core/config.py` → `memory_critical_percent` |
| Focus mode | `app/backend/watchers/comfort.py` → `def focus_plan` |
| Sleep mode for idle clients | `app/backend/watchers/comfort.py` → `def sleep_plan` |
| Per-instance audio levels (stored, reported honestly) | `app/backend/watchers/comfort.py` → `def audio_plan` |
| Safe shutdown behind an explicit confirmation | `app/backend/watchers/comfort.py` → `def shutdown_plan` |
## Rules

| Feature | Proven by |
| --- | --- |
| Rule engine that pauses, relaunches and warns | `app/backend/api/bridge.py` → `def get_rule_decisions`, `app/backend/services/fleet_features.py` → `def get_rules_overview` |
| Rules screen with its own settings | `app/backend/services/fleet_features.py` → `def update_rules`, `app/frontend/src/app.js` → `renderFleetRules()` |
| Hourly scheduler (launch a group, stop macros, ...) | `app/backend/automations/scheduler.py` → `TASK_ACTIONS`, `app/backend/services/fleet_features.py` → `def run_due_scheduled_tasks` |
| Auto-rejoin for Roblox disconnect codes | `app/backend/watchers/rejoin_rules.py` → `278`, `app/backend/api/bridge.py` → `def get_rejoin_diagnostics` |
| Restart policy after a crash or an exit | `app/backend/watchers/process_monitor.py` → `class RestartPolicy` |
## Alerts

| Feature | Proven by |
| --- | --- |
| Discord webhook | `app/backend/integrations/alerts.py` → `def discord_payload`, `app/backend/api/bridge.py` → `def update_alert_settings` |
| Phone notifications through a push relay | `app/backend/integrations/alerts.py` → `def push_payload` |
| Daily report | `app/backend/integrations/alerts.py` → `def daily_report`, `app/backend/api/bridge.py` → `def get_daily_report` |
| Secrets never echoed back to the UI | `app/backend/integrations/alerts.py` → `def redact` |
| In-app notifications | `app/backend/api/bridge.py` → `def get_notifications`, `app/backend/api/bridge.py` → `def dismiss_notification` |
| Discord presence | `app/backend/api/bridge.py` → `def get_discord_presence_status` |
## Statistics

| Feature | Proven by |
| --- | --- |
| Statistics dashboard | `app/backend/watchers/statistics.py` → `def build_statistics`, `app/backend/api/bridge.py` → `def get_statistics` |
| Hourly heatmap | `app/backend/watchers/statistics.py` → `def build_heatmap` |
| Reliability score per account | `app/backend/watchers/statistics.py` → `def reliability_table` |
| Macro success rate | `app/backend/watchers/statistics.py` → `def macro_success_rate` |
| Session comparison | `app/backend/watchers/statistics.py` → `def compare_sessions`, `app/backend/api/bridge.py` → `def compare_account_sessions` |
## System

| Feature | Proven by |
| --- | --- |
| Backups, restore and metadata export | `app/backend/api/bridge.py` → `def backup_data`, `app/backend/api/bridge.py` → `def restore_backup`, `app/backend/api/bridge.py` → `def export_metadata` |
| Update check, download and scheduled install | `app/backend/api/bridge.py` → `def check_for_updates`, `app/backend/api/bridge.py` → `def schedule_update_install` |
| Start with Windows | `app/backend/api/bridge.py` → `def set_windows_startup` |
| Diagnostics and support bundle | `app/backend/api/bridge.py` → `def get_diagnostics`, `app/backend/api/bridge.py` → `def export_support_bundle` |
| Sensitive settings refused outside OS-protected storage | `app/backend/security/redaction.py` → `def is_sensitive_key`, `app/backend/repositories/sqlite_repository.py` → `if is_sensitive_key(setting_key)` |
| Nexus kept hidden behind an environment flag | `app/backend/core/config.py` → `ASTRO_ENABLE_NEXUS` |

## Deliberately not built

| Area | Why |
| --- | --- |
| Vision: screenshots per instance, timeline, frozen-screen detector, pixel conditions, template matching, visual triggers | Excluded by the owner of the project. |
| Remote: mobile dashboard, QR pairing, read-only remote view, remote screenshot | Excluded by the owner of the project. |
| Farming a minimized window | Input is delivered to the foreground window. Doing this properly needs PostMessage/SendMessage per HWND or an isolated virtual input driver, which is a separate project. |
